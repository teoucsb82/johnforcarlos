<div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
        <div class="container">
            <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </a>
                <div class="nav-collapse">
                    <ul class="nav">
                        <li><%= link_to 'John For Carlos',  root_path, class: "brand" %></li>

                        <% if user_signed_in? && current_user.admin? %>
                            <li class="dropdown">
                                <a href="#" class="dropdown-toggle" data-toggle="dropdown">Donations
                                    <b class="caret"></b>
                                </a>
                                <ul class="dropdown-menu">                                
                                    <li><%= link_to "Manage Donations", funds_path %></li>
                                    <li><%= link_to "New Donation", new_fund_path %></li>
                                </ul>
                            </li>
                        <% else %>
                            <li><%= link_to 'Donations', new_fund_path %></li>
                        <% end %>
                        <li><%= link_to 'Messages', messages_path %></li>
                    </ul>

                    <ul class="nav pull-right">
                        <% if user_signed_in? %>
                            <li><%= link_to 'Log Out',  destroy_user_session_path, method: :delete %></li>
                        <% end %>
                        <li><%= link_to 'Admin',  new_user_session_path %></li>
                    </ul>

                </div>
        </div>
    </div>
</div>